'use client'
import { createTheme } from '@mui/material/styles';

// Customize the theme
const theme = createTheme({
  palette: {
    primary: {
      main: '#000022', 
    },
    secondary: {
      main: '#aedd2b',
    },
  },
});

export default theme;